# PostgreSQL Database Configuration
# These variables are used by docker-compose.yml for the PostgreSQL service
# Copy this file to .env.postgres if you need to override defaults

# PostgreSQL Connection Settings
POSTGRES_USER=citatum
POSTGRES_PASSWORD=citatum_password
POSTGRES_DB=citatum
POSTGRES_PORT=5432

# Database URL for Application
# When running in Docker, DATABASE_URL is automatically set by docker-compose
# For local development, use: postgresql+asyncpg://user:password@localhost:5432/database
# Note: The +asyncpg driver is required for async operations
DATABASE_URL=postgresql+asyncpg://citatum:citatum_password@postgres:5432/citatum
